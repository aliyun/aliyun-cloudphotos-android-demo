<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryLight</item>
        <item name="colorPrimaryDark">@color/primaryMedium</item>
        <item name="colorAccent">@color/primaryDark</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/AppTheme.ActionMode</item>
        <item name="actionModeCloseDrawable">@drawable/ic_arrow_back_white_24dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/primaryLight</item>
        <item name="colorPrimaryDark">@color/primaryMedium</item>
        <item name="colorAccent">@color/primaryDark</item>
    </style>

    <style name="AppTheme.NoActionBar.Dark">
        <item name="colorPrimary">@color/primaryDark</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/primaryMedium</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorPrimary">@color/primaryDarkOverlay</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.ActionMode" parent="Widget.AppCompat.ActionMode">
        <item name="actionModeBackground">@color/primaryDark</item>
        <item name="background">@color/primaryDark</item>
        <item name="android:actionModeBackground">@color/primaryDark</item>
        <item name="android:background">@color/primaryDark</item>
        <item name="android:backgroundSplit">@color/primaryDark</item>
        <item name="titleTextStyle">@style/TextAppearance.AppCompat.Title.Inverse</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/primaryDarkOverlay</item>
    </style>

</resources>
